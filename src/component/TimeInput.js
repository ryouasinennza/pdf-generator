import styled from '@emotion/styled'

export const TimeInput = styled('input')({
  width:'100%',
  padding: 0,
  border: 'none',
  borderRadius: 0,
  outline: 'none',
  background: 'none',
  backgroundColor: 'transparent'
})

